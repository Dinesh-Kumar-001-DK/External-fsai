# React Components

## How to Run
1. Create React app: `npx create-react-app myapp`
2. Replace `src/App.js` with this code
3. Run: `npm start`

## Output
- Displays "Hello Component"
- Displays "Welcome Component"