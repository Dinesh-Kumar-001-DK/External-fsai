# REST API (GET & POST)

## How to Run
1. Open terminal in this folder
2. Run: `node server.js`
3. Test with Postman or curl:
   - GET: http://localhost:3000
   - POST: http://localhost:3000 (send JSON data)

## Endpoints
- GET: Returns stored data
- POST: Adds new data